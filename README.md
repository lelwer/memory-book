# memory-book
A Python CLI tool that transforms a family memory into a 5-page, AI-illustrated storybook. This app uses the Google Gemini API to generate a rhyming story and custom images, then assembles them into a print-ready PDF.
